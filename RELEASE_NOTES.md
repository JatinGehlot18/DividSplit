## {{VERSION}}

## What's New
- Rebranded Splitkaro as DividSplit.
- Updated the Android application ID to `com.dividsplit`.

## Fixes
- Normalized release environment values so Android builds using `ENV=PROD` start successfully.
- Fixed the case-sensitive environment validation that caused the release app to close immediately.

## Notes
- The `com.dividsplit` app installs separately and does not replace existing `com.splitkaro` installations.
