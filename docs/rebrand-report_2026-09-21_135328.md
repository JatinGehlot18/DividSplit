# Rebrand Report: Splitkaro → DividSplit

**Date:** 2026-09-21
**Reason:** `splitkaro.com` was already taken. After checking availability, the brand was renamed to **DividSplit** (`dividsplit.com` — verified **free** via whois, 2026-09-21).
**Status:** ✅ Verified — `lint` 0 errors, `tsc --noEmit` clean, `jest` 8/8 passing.

> Brand-name evaluation that led here (all `.com` whois-checked):
> `splitit` **taken**, `splitshare`/`sharesplit` **taken**, entire `split*` family
> mostly taken (splitwise, splitz, splittr, splitgo…), and the `divide*`/`divvy*`
> family (27 names) almost entirely taken. **`dividsplit.com` was free and chosen.**

---

## Decisions & assumptions

| Item | Old | New |
|---|---|---|
| Brand / display name | Splitkaro | **DividSplit** |
| Android package id | `com.splitkaro` | **`com.dividsplit`** |
| iOS bundle id | RN template default | **`com.dividsplit`** (explicit in pbxproj) |
| fastlane identifiers | `com.splitkaro` | **`com.dividsplit`** |
| API domain | `https://dev-api.splitkaro.com` | **`https://dev-api.dividsplit.com`** (`.env.example`) |
| Support email | `support@splitkaro.app` | **`support@dividsplit.com`** (AccountScreen) |
| QR / share code prefix | `splitkaro:friend:` | **`dividsplit:friend:`** |
| AsyncStorage session key | `@splitkaro/session` | **`@dividsplit/session`** |

> ⚠️ Contact:
> - Register `com.dividsplit` in Play Console / App Store Connect before shipping.
> - Buy `dividsplit.com` (and reserve `dividsplit.app` / social handles).
> - Existing persisted sessions under the old key won't carry over (pre-launch, no impact).

---

## Files changed (45 entries tracked)

### Project identity
- `package.json` — `name` → `dividsplit`
- `package-lock.json` — top-level `name` entries → `dividsplit`
- `app.json` — `name` / `displayName` → `DividSplit`

### Android
- `android/app/build.gradle` — `namespace` + `applicationId` → `com.dividsplit`
- `android/settings.gradle` — `rootProject.name` → `DividSplit`
- `android/app/src/main/java/com/splitkaro/` → **`com/dividsplit/`** (dir moved via `git mv`)
  - `MainActivity.kt` — `package com.dividsplit`; `getMainComponentName()` → `"DividSplit"`
  - `MainApplication.kt` — `package com.dividsplit`
- `android/app/src/main/res/values/strings.xml` — `app_name` → `DividSplit`

### iOS
- `ios/Splitit/` → **`ios/DividSplit/`** (moved via `git mv`, incl. `Images.xcassets`)
- `ios/Splitit.xcodeproj` → **`ios/DividSplit.xcodeproj`** (moved via `git mv`)
  - `project.pbxproj` — target/group/product names, `PRODUCT_NAME`, Pods target refs, plist paths → `DividSplit`; `PRODUCT_BUNDLE_IDENTIFIER = "com.dividsplit"` (Debug + Release)
  - `xcshareddata/xcschemes/Splitit.xcscheme` → **`DividSplit.xcscheme`**
- `ios/Podfile` — target `'DividSplit'`
- `ios/DividSplit/Info.plist` — `CFBundleDisplayName` + camera usage string → `DividSplit`
- `ios/DividSplit/LaunchScreen.storyboard` — launch label → `DividSplit`
- `ios/DividSplit/AppDelegate.swift` — RN module name → `"DividSplit"`

### fastlane
- `fastlane/Appfile` — `app_identifier` / `package_name` → `com.dividsplit`
- `fastlane/Fastfile` — iOS `scheme: "DividSplit"`

### Domains / app code
- `.env.example` — `API_URL` → `https://dev-api.dividsplit.com`
- `App.tsx` — header doc comment → `DividSplit`
- `src/screens/AccountScreen.tsx` — `support@dividsplit.com`; footer → `DividSplit · built with React Native`
- `src/screens/CreateGroupScreen.tsx` — WhatsApp share → "Join my group on DividSplit!"
- `src/screens/ScanCodeScreen.tsx` — `CODE_PREFIX` → `dividsplit:friend:` + doc comment + share strings
- `src/auth/sessionStorage.ts` — `SESSION_KEY` → `@dividsplit/session`
- `src/api/client.ts` — backend comment → `DividSplit backend`

### Docs & CI
- `README.md` — title, About, getting-started → `DividSplit`
- `.github/workflows/deploy-prod.yml` — artifact `dividsplit-prod-…`, release `"DividSplit …"`
- `.github/workflows/deploy-staging.yml` — artifact `dividsplit-staging-…`

---

## Intentionally unchanged

- `docs/test-report_2026-09-17_215505.md` — timestamped historical test report (under old brand).
- `android/app/build/`, `android/.gradle/`, `android/app/.cxx/` — build artifacts; regenerate on next build.
- `graphify-out/` — knowledge graph; **regenerated** after rename (`graphify update .` → 416 nodes, 996 edges).
- The repo directory/name (`Splitkaro-frontend`) — outside tracked code; rename the GitHub repo / folder if desired.

---

## Follow-up required (not run)

1. **iOS:** delete stale `ios/Podfile.lock` / `ios/Pods/` if present, then `bundle exec pod install`.
2. **Clean native rebuild** (`cd android && ./gradlew clean`) to drop stale `com/splitkaro` artifacts.
3. Buy the domain (`dividsplit.com`), register package/bundle id `com.dividsplit`, claim app-store/play-store entries.
4. Re-render app icon/screenshots if artwork contains the old wordmark.

---

## Verification

| Check | Result |
|---|---|
| `npm run lint` | 0 errors (217 pre-existing style warnings) |
| `npx tsc --noEmit` | clean |
| `npm test -- --ci` | 3 suites / 8 tests passed |
| `git grep -i splitkaro` (tracked source) | 0 matches |
| `git grep -i splitit` (tracked source) | 0 matches |
| `graphify update .` | rebuilt (416 nodes, 996 edges) |